# Mail Triage CLI Project Site

This repo ships a simple GitHub Pages site from `docs/`.

## Deploy

1. In GitHub, open `Settings -> Pages`
2. Set `Build and deployment` to `GitHub Actions`
3. Push to `main`

The included workflow publishes `docs/` automatically.
